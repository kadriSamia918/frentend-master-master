export class Client{
    //  put your models either interface or classs not both. if you need both put an interface and implement the interface.
    name:string;
    email:string;
    phoneNumber:string;
    address:string;
    username:string;
 }